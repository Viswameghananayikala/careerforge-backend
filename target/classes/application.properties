# Server
server.port=8080

# MySQL Database (same as your .env)
spring.datasource.url=jdbc:mysql://localhost:3306/careerforgedb?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Meghana@0706
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=CareerForgeSecretKey2025SuperLongSecureKeyForJWTSigning
jwt.expiration=86400000

# Admin Credentials (seeded on startup)
admin.email=admin@careerforge.com
admin.password=CareerForge@2025

# Frontend URL (for CORS)
frontend.url=http://localhost:5173

# Logging
logging.level.com.careerforge=INFO
